# 2026-07-26 工作记录

## android-adskip 模块化重构

### 背景
仓库中存在 5 个文件有未解决的 Git 合并冲突（AdSkipAccessibilityService、FloatWindowManager、KeepAliveService、StatsStore、AppState），代码无法编译。同时 AdSkipAccessibilityService.kt（394 行）和 MainActivity.kt（540 行）职责过重。

### 完成的工作

1. **解决了所有合并冲突** — 5 个文件全部清除 `<<<<<<< HEAD` / `=======` / `>>>>>>>` 标记，选择了功能更完善的 HEAD 分支版本。

2. **拆分了无障碍服务** — 原来 394 行的 AdSkipAccessibilityService 拆分为：
   - `service/AdSkipAccessibilityService.kt` — 精简为纯调度器（~95 行）
   - `service/matcher/RuleMatcher.kt` — 规则匹配引擎
   - `service/capturer/CaptureManager.kt` — 捕获模式管理
   - `service/executor/ClickExecutor.kt` — 点击执行 + 冷却 + 统计
   - `service/guard/FilterGuard.kt` — 黑白名单过滤
   - `util/AccessibilityUtil.kt` — 通用节点操作工具

3. **拆分了 MainActivity** — 原来 540 行的主界面拆分为：
   - `ui/MainActivity.kt` — 精简协调器（~250 行）
   - `ui/components/KeywordSection.kt` — 关键词管理
   - `ui/components/RuleSection.kt` — 规则编辑/删除
   - `ui/components/FilterSection.kt` — 过滤列表 + 应用选择器
   - `ui/components/LogSection.kt` — 点击日志
   - `ui/components/SyncSection.kt` — GitHub 同步

4. **修复了 BootReceiver bug** — `isAccessibilityEnabled()` 改用完整类名而非 manifest 短格式。

5. **增强了 Logger** — 从空壳对象扩展为带级别控制的完整日志工具。

### 编译与安装
- 修复 2 个编译错误：`ClickExecutor` 中 `logText` 作用域问题、`MainActivity` 缺少 `lifecycleScope` import
- Debug APK 编译成功（6.7MB），已安装到设备 ec9f07a3
- ZIP 密码 `Mm123456.` 解压 keystore 成功，但 keystore 密码存储在 GitHub Secrets 中无法获取，目前用 Debug 签名

### 文件统计
- 原有: 14 个 Kotlin 文件
- 现有: 27 个 Kotlin 文件
- 新增模块目录: service/{matcher,capturer,executor,guard}, ui/components
